const Soma = (a, b) => a + b
const Subtracao = (a, b) => a - b
const Multiplicacao = (a, b) => a * b
const Divisao = (a, b) => a / b

module.exports = {
    Soma,
    Subtracao,
    Multiplicacao,
    Divisao
}